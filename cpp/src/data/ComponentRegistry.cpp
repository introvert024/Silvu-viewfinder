#include "ComponentRegistry.h"

// Implementation of ComponentRegistry
// The singleton instance is defined inline in the header since we use a local static variable.
// Registration methods are inline or template definitions.
